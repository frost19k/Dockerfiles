# pyenv

Here is where I manage & conduct experiments to improve the Docker Container for [pyenv](https://github.com/pyenv/pyenv).

### Run the container
```bash
❯ docker run -it --rm \
  -v "${PWD}":'/pyenv' \
  frost19k/pyenv
```
